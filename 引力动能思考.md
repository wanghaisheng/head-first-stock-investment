你的直觉里有一半很扎实，有一半会和「顶底结构」拧着。可以拆开看，再给一个更干净的三层设计。

---

## 你现在的说法

| 量 | 你的理解 |
|----|----------|
| 最高−均线 `highDiff` | 动能（往上） |
| 最低−均线 `lowDiff` | 引力（往下） |
| 收盘−均线 vs 回撤基数 | 回撤程度 |
| 回撤基数 | 时间窗内 `highDiff` 的最大值 |

---

## 哪里合理

1. **窗内 `max(highDiff)` 作「冲高基数」**  
   很合理：这是「这段时间里相对均线最猛的一次上冲」。  
   用  
   \(\text{冲高回撤%} = \dfrac{\max(\text{highDiff}) - \text{closeDiff}}{\max(\text{highDiff})}\)（仅当 max>0）  
   表示「冲高溢价吐了多少」——这和你们一直说的「正差从极值回落」一致，也比单纯用股价 Peak 更贴「差额方法论」。

2. **收盘差是「结算」**  
   最高/最低是盘中极值，收盘是多空谁留下。用收盘差相对冲高基数，比只看 high/low 更稳。

3. **最低差刻画「下方压力/失守」**  
   `lowDiff` 很负 = 盘中一度深度跌破均线，确实像「往下的力在试探」。  
   若 `lowDiff` 抬升（负差收敛），像下方引力在减轻。

---

## 哪里容易拧巴

若直接规定：

- **highDiff 大 = 动能强**  
- **lowDiff 小（更负）= 引力强**

会和经典顶底语义冲突：

| 场景 | highDiff | 若=动能 | 结构上更常见的含义 |
|------|----------|---------|-------------------|
| 主升、创新高 | 很大 | 很强动能 | 也对：多头强 |
| **顶部冲高** | 也很大 | 也说动能很强 | 更应警惕「拉伸过度、易回落」 |
| 阴跌、低位 | highDiff 不大 | 动能弱 | 可能正在筑底，未必没有修复机会 |

也就是说：**highDiff 大 =「上冲幅度大」**，不等于「现在仍适合做多的动能」。  
它同时可能是「强势」或「顶部过热」，必须靠 **路径（扩大还是缩小）** 和 **收盘有没有留住** 才能定方向。

同理：`lowDiff` 很负可以是「恐慌探底」（后续可收敛做多），也可以是「趋势破位」（引力兑现）。同样不能只看水平，要看是在 **加深还是收敛**。

---

## 更合理的三层理解（建议）

把「位置、路径、回撤」拆开，比把 high=动能、low=引力绑死更稳：

### ① 盘中攻防（你提的 high/low 最适合放这里）

| 量 | 建议读法 |
|----|----------|
| `highDiff` | **上冲幅度**（多头当天推到多远） |
| `lowDiff` | **下探幅度**（空头当天砸到多深） |
| 两者一起 | 看波动谁主导，而不是直接当买卖力 |

可选派生：

- 上冲留存：`closeDiff / highDiff`（highDiff>0 时）——冲高后收盘留住多少  
- 下探修复：若 lowDiff<0，`(closeDiff - lowDiff) / |lowDiff|`——盘中深砸后收回多少  

### ② 路径力（真正的「往上的力 / 往下的力」）

仍用 **窗口内变化**，而不是水平绝对值：

| 路径 | 含义 |
|------|------|
| closeDiff、highDiff **走强/扩大** | 往上的路径（偏走强） |
| closeDiff、highDiff **走弱/缩小** 或 lowDiff **加深** | 往下的路径（偏回落） |
| lowDiff **收敛**（从很负往上抬） | 下方压力减轻，修复中 |

这与 v0.18 路径型引力/动能一致，也避免「大涨在均线上方却显示强引力」那种语感错位。

### ③ 两种回撤（建议并列，不要混成一个）

| 名称 | 基数 | 公式（示意） | 回答的问题 |
|------|------|----------------|------------|
| **价格回撤** | 窗内最高价 | \((P_{high}-P_{close})/P_{high}\) | 价格从高点掉了多少 |
| **冲高差额回撤** | 窗内 \(\max(highDiff)\) | \((\max H_{diff}-C_{diff})/\max H_{diff}\) | 相对均线的上冲溢价吐了多少 |

你提的「回撤基数 = 区间最高价差额的最大值」→ 正好对应 **冲高差额回撤**，应保留并写进明细；价格回撤继续服务风控 R。

---

## 和「high=动能、low=引力」怎么折中

更干净的命名：

| 不建议 | 建议 |
|--------|------|
| highDiff = 动能 | highDiff = **上冲幅度**；动能 = 上冲/收盘差的 **走强路径** |
| lowDiff = 引力 | lowDiff = **下探幅度**；引力 = 下探加深或正差回落的 **走弱路径** |
| 一个回撤混用 | **价格回撤** + **冲高差额回撤** 分列 |

这样：

- 你的「基数用 max(highDiff)」完全保留，而且更贴方法论；  
- 又不会在顶部把「巨大的 highDiff」误读成「还要加仓的动能」。

---

## 小结

- **对**：窗内 \(\max(highDiff)\) 作冲高基数；收盘差相对它形成回撤程度；low 描述下方压力。  
- **慎**：把每日 high/low **水平**直接等于动能/引力——更适合叫上冲/下探，**动能/引力应跟路径（变大还是变小）走**。  
- **更好**：三层——盘中攻防（high/low）→ 路径力（差额怎么变）→ 双回撤（价格 + 冲高差额）。

若你认可这套命名与双回撤，我可以按此改侧栏文案、明细列和引力/动能说明（计算上冲高基数你们已有雏形，主要是对齐定义与展示）。




```
路径力在代码里是对**某一个时间窗口**（短线 7 日、中线 30 日等）算出来的两个 0～100 分：**往下的力 G（回落路径）**、**往上的力 K（走强路径）**。下面按「先定义量 → 再打分 → 再比大小 → 再算 Δ」说明。

---

## 1. 窗口与归一化尺度

取窗口内 K 线 \(w_0,\ldots,w_{n-1}\)（旧→新），最新一根为 \(w_{n-1}\)。

**尺度**（把「差多少价」变成相对幅度，避免高价股分数爆炸）：

\[
\text{scale} = \max(0.08 \times \text{Close}_{n-1},\; 3)
\]

即约 **8% 现价** 与 **3 个点** 取大。

---

## 2. 路径相关的中间量

| 符号 | 定义 | 含义 |
|------|------|------|
| \(C,H,L\) | 最新 `closeDiff / highDiff / lowDiff` | 结算 / 上冲 / 下探 |
| \(p_C,p_H,p_L\) | 窗末 − 窗首 同名字段 | **整段路径**净变化 |
| \(d_C,d_H,d_L\) | 最新 − 前一日 | **近 1 日**速度 |
| \(a_C,\ldots\) | \(d^{(t)} - d^{(t-1)}\) | **加速度**（速度是否加快） |
| \(\text{priceDd}\) | \((\text{窗内最高价}-\text{Close})/\text{窗内最高价}\times 100\%\) | 窗内**价格回撤** |
| \(\text{premDd}\) | \((\max H_{\text{diff}} - C)/\max H_{\text{diff}}\times 100\%\)（\(\max H>0\)） | **冲高差额回撤**（基数=窗内最大上冲） |

注意：**上冲/下探的绝对水平不直接当 G/K**；主要进分的是 \(p,d,a\) 和两种回撤。

---

## 3. 往下的力 \(G\)（回落路径）— 累加后截断到 \([0,100]\)

只在「走弱/回撤扩大」时加分：

| 条件 | 加分（再与上限取 min） |
|------|------------------------|
| \(p_H < 0\)（上冲整段回落） | \(\min(28,\; 35\cdot\|p_H\|/\text{scale})\) |
| \(p_C < 0\)（结算整段走弱） | \(\min(28,\; 35\cdot\|p_C\|/\text{scale})\) |
| \(d_C < 0\)（近 1 日结算走弱） | \(\min(18,\; 28\cdot\|d_C\|/\text{scale})\) |
| \(d_H < 0\) | \(\min(12,\; 20\cdot\|d_H\|/\text{scale})\) |
| \(a_C < 0\) 且 \(d_C < 0\)（走弱在加速） | \(\min(12,\; 18\cdot\|a_C\|/\text{scale})\) |
| \(\text{priceDd} \ge 3\%\) | \(\min(22,\; 1.4\cdot\text{priceDd})\) |
| \(\text{premDd} \ge 25\%\) | \(\min(15,\; 0.35\cdot(\text{premDd}-20))\) |

直觉：**整段或近端上冲/结算在变差，或价格/冲高溢价回吐变大 → G 升高。**

---

## 4. 往上的力 \(K\)（走强路径）

| 条件 | 加分 |
|------|------|
| \(p_C > 0\)（结算整段走强） | \(\min(28,\; 35\cdot p_C/\text{scale})\) |
| \(p_L > 0\)（下探抬升=低点相对均线变好） | \(\min(18,\; 28\cdot p_L/\text{scale})\) |
| \(d_C > 0\) | \(\min(18,\; 28\cdot d_C/\text{scale})\) |
| \(d_L > 0\) | \(\min(12,\; 20\cdot d_L/\text{scale})\) |
| \(a_C > 0\) 且 \(d_C > 0\) | \(\min(12,\; 18\cdot a_C/\text{scale})\) |
| \(\text{priceDd}\ge 8\%\) 且 \(d_C>0\)（深回撤后近端修复） | \(\min(18,\; 1.1\cdot(\text{priceDd}-5))\) |
| 窗首 \(C_0<0\) 且 现 \(C>C_0\)（负差收敛） | \(\min(20,\; 30\cdot(C-C_0)/\text{scale})\) |

直觉：**结算走强、下探收敛、深回撤后往上修 → K 升高。**

---

## 5. 持仓成本（微调，不主导）

- 浮亏 \(\le -10\%\) 且 \(K>15\)：\(K \leftarrow \min(100, K+6)\)  
- 浮盈 \(\ge 12\%\)、几乎无价格回撤、且 \(p_C<0\)：\(G \leftarrow \min(100, G+6)\)

最后：

\[
G,K \leftarrow \mathrm{clamp}(\cdot,\,0,\,100)
\]

---

## 6. 倾向（谁更大）

\[
\begin{align*}
G &\ge K + 12 &&\Rightarrow \text{偏回落}\\
K &\ge G + 12 &&\Rightarrow \text{偏走强}\\
&\text{否则} &&\Rightarrow \text{震荡}
\end{align*}
\]

**路径优先覆盖**（避免和近端走势打架）：

- \(p_C>0\)、价格回撤 \(<4\%\)、且 \(K \ge G-5\) → 强制 **偏走强**  
- \(p_C<0\)、价格回撤 \(\ge 5\%\)、且 \(G \ge K-5\) → 强制 **偏回落**

---

## 7. 力的变化 Δ 与加速度（表上的「Δ +55 ↑加速」）

对同一窗口，分别用：

- 全窗 \(w_{0..n-1}\) → \((G_t, K_t)\)  
- 去掉最后一天 → \((G_{t-1}, K_{t-1})\)  
- 再去掉一天 → \((G_{t-2}, K_{t-2})\)

则：

\[
\Delta G = G_t - G_{t-1},\quad
A_G = \Delta G - (G_{t-1}-G_{t-2})
\]

\(K\) 同理。文案规则大致是：

- \(|\Delta|<1.5\)：几乎没动  
- \(\Delta>0\) 且 \(A>1\)：增强且加速  
- \(\Delta>0\) 且 \(A<-1\)：仍在增强但放缓（渐变）  
- 对减弱对称  

---

## 8. 和「上冲/下探水平」的关系（重要）

| 量 | 是否直接等于路径力 |
|----|-------------------|
| 上冲 \(H\) 很大 | **否** → 只是幅度；若 \(p_H,p_C\) 仍在升，进的是 **K**，不是 G |
| 下探 \(L\) 很负 | **否** → 幅度；若 \(p_L\) 抬升、负差收敛，进的是 **K** |
| 路径力 | **变化 + 回撤是否扩大** 的加权分 |

所以量化公式是：

\[
G = f(\text{走弱的 }p,d,a,\;\text{priceDd},\;\text{premDd}),\quad
K = f(\text{走强的 }p,d,a,\;\text{修复项})
\]

而不是 \(G \propto -L\)、\(K \propto H\)。

---

## 9. 一小例子（帮助建立数量级）

设 `scale ≈ 5`，窗内结算差从 +2 落到 −3，则 \(p_C=-5\)：

\[
G \mathrel{+}= \min(28,\; 35\times 5/5)=\min(28,35)=28
\]

若近 1 日再 \(d_C=-2\)：

\[
G \mathrel{+}= \min(18,\; 28\times 2/5)=\min(18,11.2)=11.2
\]

若同时价格回撤 6%：

\[
G \mathrel{+}= \min(22,\; 1.4\times 6)=8.4
\]

仅这三项就已到约 **47** 分，容易显示「往下的力中等偏强」。

反过来，若窗内 \(p_C=+5\)、几乎无回撤，则主要加在 **K** 上，倾向会偏走强——这正是「近几日上涨却不应再被标成强引力」的设计意图。

---

**一句话：**  
路径力 = 在**当前时间焦点窗口**内，用结算/上冲/下探的**窗内变化、近1日速度、加速度**，再加上**价格回撤**与**冲高差额回撤**，按上表加权得到 \(G,K\in[0,100]\)，再用差值 12 分定倾向，并用相邻两日的 \(G,K\) 算 Δ 与加速度。  

若你希望改成「完全可解释的归一化公式」（例如每项权重可配置、或改成 0～1 的概率风格），可以指定偏好，我可以按同一套变量改成更透明的版本。
```

